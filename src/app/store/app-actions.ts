import { createAction, props } from '@ngrx/store';

export const setClearFilter = createAction('[App] Set Clear Filter');
export const clearFilterHandled = createAction('[App] Clear Filter Handled');
